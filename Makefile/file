CC = gcc
CFLAGS = -Iinclude -Wall -g

SRC = src/main.c src/train.c src/vagon.c
TARGET = train_simulation

all: $(TARGET)

$(TARGET): $(SRC)
	$(CC) $(CFLAGS) $(SRC) -o $(TARGET)

clean:
	rm -f $(TARGET) *.o
